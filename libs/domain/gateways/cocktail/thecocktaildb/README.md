# domain-gateways-cocktail-thecocktaildb

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build domain-gateways-cocktail-thecocktaildb` to build the library.

## Running unit tests

Run `nx test domain-gateways-cocktail-thecocktaildb` to execute the unit tests via [Jest](https://jestjs.io).
